Param:
  buildId: "2323"
  imageName: "myapp-{{ Param.buildId }}"
  fullImage: "{{ Param.imageName }}:latest"
Executors:
  local:
    type: local
    config:
      shell: bash
Graph: |
  stateDiagram-v2
    [*] --> Task1
Nodes:
  Task1:
    executor: local
    steps:
      - name: step1
        run: "echo 'test'"
