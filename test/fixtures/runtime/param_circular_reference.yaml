Param:
  a: "{{ Param.b }}"
  b: "{{ Param.c }}"
  c: "{{ Param.a }}"
Executors:
  local:
    type: local
    config:
      shell: bash
Graph: |
  stateDiagram-v2
    [*] --> Task1
Nodes:
  Task1:
    executor: local
    steps:
      - name: step1
        run: "echo 'task1'"
